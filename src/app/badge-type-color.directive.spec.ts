import { BadgeTypeColorDirective } from './badge-type-color.directive';

describe('BadgeTypeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BadgeTypeColorDirective();
    expect(directive).toBeTruthy();
  });
});
